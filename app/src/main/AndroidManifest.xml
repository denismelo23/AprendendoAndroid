<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalhofatec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tela.ListarBanhosActivity"></activity>
        <activity android:name=".tela.CadastroBanhoActivity" />
        <activity android:name=".tela.ListarAnimaisActivity" />
        <activity android:name=".tela.CadastroAnimalActivity" />
        <activity android:name=".tela.MenuNavegacaoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tela.CadastroFuncionarioActivity" />
        <activity android:name=".tela.ListarFuncionariosActivity" />
    </application>

</manifest>